// ***********************************************************
// This file is no longer needed with Cypress 10+ and modern preprocessor.
// Configuration is now handled in cypress.config.js
// Keeping this file for backwards compatibility but it's not actively used.
// ***********************************************************

module.exports = (on, config) => {
  // No-op: Configuration moved to cypress.config.js setupNodeEvents
  return config
}

